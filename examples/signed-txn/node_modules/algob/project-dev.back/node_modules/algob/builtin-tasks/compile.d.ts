import { CompileOp } from "../lib/compile";
export default function (): void;
export interface TaskArgs {
    force: boolean;
}
export declare function compile(force: boolean, op: CompileOp): Promise<void>;
