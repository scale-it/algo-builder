"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.partitionByFn = void 0;
function getLast(ts) {
    return ts[ts.length - 1];
}
// Takes out the rightest child of a list
// `[[a, b], [c, d]]` would produce `d`
function getLastDeepChild(ts) {
    return getLast(getLast(ts));
}
function partitionByFn(f, input) {
    if (input.length === 0) {
        return [];
    }
    var out = [[input[0]]];
    for (const current of input.slice(1)) {
        const last = getLastDeepChild(out);
        if (f(last, current)) {
            out.push([current]);
        }
        else {
            out[out.length - 1].push(current);
        }
    }
    return out;
}
exports.partitionByFn = partitionByFn;
//# sourceMappingURL=lists.js.map