import type { Algodv2, CompileOut } from "algosdk";
import type { ASCCache } from "../types";
export declare const tealExt = ".teal";
export declare class CompileOp {
    algocl: Algodv2;
    cacheAssured: boolean;
    constructor(algocl: Algodv2);
    ensureCompiled(filename: string, force: boolean): Promise<ASCCache>;
    readTealAndHash(filename: string): [string, number];
    readArtifact(filename: string): Promise<ASCCache | undefined>;
    callCompiler(code: string): Promise<CompileOut>;
    compile(filename: string, tealCode: string, tealHash: number): Promise<ASCCache>;
    writeFile(filename: string, content: string): void;
}
